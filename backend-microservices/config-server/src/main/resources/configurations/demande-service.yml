server:
  port: 8082

spring:
  application:
    name: demande-service
  data:
    mongodb:
      uri: mongodb://localhost:27017/demande_db

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
